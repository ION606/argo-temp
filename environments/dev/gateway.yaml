apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
    name: blahaj-gw
    namespace: argocd
spec:
    gatewayClassName: istio
    listeners:
        - name: http
          port: 80
          protocol: HTTP
